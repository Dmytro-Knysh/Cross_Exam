import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.exam_cross',
  appName: 'exam_cross',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
